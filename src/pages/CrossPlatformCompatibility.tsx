
import React from 'react';
import { Layout } from '@/components/Layout';
import { CrossPlatformDashboard } from '@/components/compatibility/CrossPlatformDashboard';

export default function CrossPlatformCompatibility() {
  return (
    <Layout>
      <CrossPlatformDashboard />
    </Layout>
  );
}
