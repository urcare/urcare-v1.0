
import React from 'react';
import { Layout } from '@/components/Layout';
import { DeviceIntegrationDashboard } from '@/components/device-integration/DeviceIntegrationDashboard';

const DeviceIntegration = () => {
  return (
    <Layout>
      <DeviceIntegrationDashboard />
    </Layout>
  );
};

export default DeviceIntegration;
